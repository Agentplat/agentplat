# Agent Morphogenesis Beta 1 Mesh partition-recovery runbook

A minority view cannot authorize Morphogenesis. Retain signed evidence and
return no collective authorization below three independent supporters in the
four-validator profile.

After healing, retry authenticated delivery, drain durable inbox/outbox rows,
verify message de-duplication and reconstruct the application certificate from
the pinned membership epoch. Stale-epoch, colluding independence groups and
conflicting evidence remain rejected. Mesh evidence never grants execution
authority without the separate current decision mandate.
