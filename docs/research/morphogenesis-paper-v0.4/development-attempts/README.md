# Development passes

Two 96-case engineering passes preceded the retained pilot and held-out evaluation. Both completed; they are excluded from all held-out counts. The first preceded the explicit query ceiling/per-query trace. Its exploratory scores.json was added after the raw manifest and is an auxiliary analysis file, not part of that raw manifest. The outer ZIP manifest covers every retained file. The second pass includes the revised instrumentation; its analysis is retained separately.

The accepted raw bundles for the final paper are v0.2/pilot, v0.3/integration, v0.4/pilot and v0.4/evaluation. Their dedicated verifiers check complete coverage. Development files are retained for transparency and are not relabeled with later code hashes.
