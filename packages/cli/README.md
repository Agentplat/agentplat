# @agentplat/cli

CLI contract surface for AgentPlat project tooling.

This package defines command names, command context, command results and command handlers. It is the foundation for scaffolding, validation and operational tooling around AgentPlat projects.
