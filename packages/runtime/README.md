# @agentplat/runtime

Agent runtime contracts for production agentic systems.

This package defines how agents are described, how execution context is passed into providers, how run results are represented and how streaming events are surfaced.

It is the interface boundary between product code, model providers, tools, policies and workflow orchestration.
